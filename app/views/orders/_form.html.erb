<%= form_for(@order) do |f| %>
  <% if @order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% @order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "姓名" %><br>
    <%= f.text_field :name, size: 40 %>
  </div>
  <div class="field">
    <%= f.label "地址" %><br>
    <%= f.text_area :address, rows: 3, colos: 40 %>
  </div>
  <div class="field">
    <%= f.label "电子邮件" %><br>
    <%= f.text_field :email, size: 40 %>
  </div>
  <div class="field">
    <%= f.label "支付类型" %><br>
    <%= f.select :pay_type, PaymentType.names, prompt: "请选择一种支付方式" %>
  </div>
  <div class="actions">
    <%= f.submit "现在付款" %>
  </div>
<% end %>
